# Food-Delivery-on-n8n
This project demonstrates a simple but complete food-ordering backend workflow built using n8n. A frontend UI (HTML/CSS/JS) deployed on Netlify collects food orders, sends them to an n8n Webhook, processes the data through a Code node, and stores finalized orders in MongoDB.
